import { Section } from "@/components/layout/Section";

export function Skills() {
  return (
    <Section id="skills" title="Skills">
      <p className="text-muted">
        Content coming Day 3 — current stack (React/Next.js, Python, Vue3,
        PostgreSQL) as skill tags instead of the old progress bars.
      </p>
    </Section>
  );
}
